{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build && install",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && cmake .. && make && make install",
            "group": {
                "kind": "build",
                "isDefault": true 
            }
        }
    ]
}